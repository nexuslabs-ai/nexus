services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: context-engine-db
    environment:
      POSTGRES_USER: context_engine
      POSTGRES_PASSWORD: context_engine_dev
      POSTGRES_DB: context_engine
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U context_engine']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
