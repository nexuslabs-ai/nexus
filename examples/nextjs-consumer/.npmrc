# The exported design system resolves from the local Verdaccio registry; every
# other dependency (react, next, @nexus_ds/core, @radix-ui/*, ...) comes from the
# default (real) npm registry. A real consumer points @acme at their own registry.
@acme:registry=http://localhost:4873
