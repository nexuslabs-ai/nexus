import { CreateWorkspace } from '../../_create/create-workspace';

export const metadata = { title: 'Tokens — Nexus Design System' };

export default function TokenPage() {
  return <CreateWorkspace view="tokens" />;
}
