{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "!CHANGELOG.md", "!CSP.md"],
      "outputs": [".next/**", "!.next/cache/**"]
    }
  }
}
