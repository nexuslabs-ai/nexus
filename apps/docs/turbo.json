{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/package.json",
        "!CHANGELOG.md",
        "!CSP.md"
      ],
      "outputs": [".next/**", "!.next/cache/**", "next-env.d.ts"]
    }
  }
}
